import 'package:flutter/material.dart';

class LanguageButton extends StatelessWidget {
  final Map questionText;

  LanguageButton({required this.questionText});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
